/**
 *
 * @param s
 * @returns
 */
function longestPalindrome(s: string): string {
    let Resleft = 0;
    let Resright = 0;
    let maxLength = 0;

    return "1";
}

function main() {
    let str = "babad";
    longestPalindrome(str);
}

main();

export default longestPalindrome;
